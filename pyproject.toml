[build-system]
<<<<<<< HEAD
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ua-batteries"
version = "0.1.0"
description = "Automating production of optimal times to buy/sell energy from Ukrainian government with user-determined buy/sell cycle setup"
readme = "README.md"
requires-python = ">=3.10"

authors = [{name = "Roman Demianovych", email = "roman.demianovych@gmail.com"}]

dependencies = [
    "requests>=2.32,<3.0",
    "pandas>=2.1,<3.0",
    "beautifulsoup4>=4.14,<5.0",
    "lxml>=4.9,<5.0",
    "numpy>=1.24,<3.0"
    ]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "black>=23.0",
    "flake8>=6.0",
    "flake8-docstrings>=1.7",
    "pep8-naming>=0.13"
    ]

[tool.flake8]
max-line-length = 120
=======
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ua-batteries"                    # package distribution name (can have hyphen)
version = "0.1.0"
description = "A lightweight Python package for analyzing and modeling battery data."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.8"
dependencies = []

[project.urls]
Homepage = "https://github.com/mathseuler/ua-batteries"
Issues = "https://github.com/mathseuler/ua-batteries/issues"

# ---- Tell setuptools exactly where your importable package lives ----
[tool.setuptools.packages.find]
where = ["."]
include = ["ua_batteries*"]
>>>>>>> aedc33a734e48af1c64ced4cc6db287d126aabfd
